{
  "name": "migration-rollback",
  "version": "1.0.0",
  "description": "Rollback procedures with backup file restoration",
  "main": "rollback-migration.js",
  "scripts": {
    "rollback": "node rollback-migration.js",
    "rollback:selective": "node rollback-migration.js --selective",
    "cleanup": "node rollback-migration.js --cleanup"
  },
  "dependencies": {
    "glob": "^10.3.10"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}