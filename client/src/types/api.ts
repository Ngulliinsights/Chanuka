/**
 * Standardized API Type Definitions - Compatibility Layer
 * 
 * This module re-exports types from the unified core/api/types system.
 * It maintains backward compatibility with existing code.
 * 
 * @deprecated Use @client/core/api/types directly for new code
 */

// Re-export all types from the unified system
export * from '../core/api/types';
