/**
 * Unified Type Definitions for API Client Architecture
 * 
 * This module re-exports types from the modular type system for backward compatibility.
 * For new code, import directly from @client/core/api/types (modular index).
 * 
 * @deprecated Use direct imports from modular types instead
 */

// Re-export everything from the modular types index
export * from './types/index';
