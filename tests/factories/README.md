/**
 * Test Data Factories - Phase 2 Placeholder
 * 
 * This directory will contain feature-specific mock factories in Phase 2.
 * 
 * Examples:
 * - bill-factory.ts: createSimpleBill(), createBillWithAmendments(), etc.
 * - user-factory.ts: createUser(), createAdmin(), createExpert(), etc.
 * - analysis-factory.ts: createAnalysisResult(), createComplexAnalysis(), etc.
 * 
 * For now, use global.testUtils.createMock*() for basic mocks.
 */
